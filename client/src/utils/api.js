export const prefix = "/api/v1";

export default {
    users: `${prefix}/users`,
    pins: `${prefix}/pins`,
    saves:`${prefix}/saves`,
    upload:`${prefix}/upload`,
    comments:`${prefix}/comments`,
}